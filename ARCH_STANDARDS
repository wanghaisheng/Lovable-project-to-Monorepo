这是为您深度整合、最终定稿的 2 份核心技术架构规范文档。

这两份文档确立了 **“逻辑全抽离，UI 抽象化，存储契约化”** 的顶层设计，确保一套核心逻辑（Unified Core）能同时驱动 **Astro Web、Capacitor PWA、Ionic Hybrid** 以及 **Expo React Native** 应用。

---

### 文档 1：技术架构与全场景解耦规范 (ARCH_STANDARDS.md)

```markdown
# 技术架构与全场景解耦规范 (Unified Core & Multi-Platform Architecture)

## 1. 核心哲学：三位一体 (The Trinity)
系统通过单一的“大脑” (`packages/core`) 驱动所有端，通过“适配器”处理环境差异。
1.  **逻辑全抽离 (Logic Fully Decoupled)**: 业务规则被抽象为 Procedures，与传输层和运行环境物理隔离。
2.  **UI 抽象化 (UI Abstracted)**: 表现层仅作为状态投影，使用设计令牌（Tokens）和虚拟化技术。
3.  **存储契约化 (Storage Contracted)**: 业务逻辑仅通过 `IStorageAdapter` 接口操作数据，屏蔽具体数据库 SDK。

## 2. 核心分层架构
### 2.1 表现层 (UI Layer - React/Ionic/RN)
*   **Web (Astro)**: 高性能 SEO 首屏，流量驱动型 SSG。
*   **PWA (Ionic + Capacitor)**: 混合原生能力。使用 `vite-plugin-pwa` 管理离线生命周期，使用 `Capacitor` 桥接原生插件（扫码、通知）。
*   **Native (Expo/RN)**: 极致原生性能，直接调用原生组件。
*   **性能组件**: 大数据长列表统一使用 `Virtua` 进行零配置虚拟化渲染。

### 2.2 动作层 (Action Layer - API & Procedures)
不再区分 Hooks 和 Services，统一抽象为 Action 层：
*   **传输透明**: 
    *   **tRPC 模式**: Web 端通过远程调用连接 Cloudflare Workers。
    *   **Local 模式**: RN 或离线 PWA 直接在本地注入逻辑并执行。
*   **原则**: 禁止在 Action 层引用 `window`, `document` 或任何平台特有的全局变量。

### 2.3 持久层 (Storage Adapters)
*   **契约化**: 定义 CRUD、Auth、Query 接口。
*   **实现实现**: `SupabaseAdapter` (云端), `D1Adapter` (边缘), `SqliteAdapter` (PWA/原生本地), `AsyncStorageAdapter` (移动端轻量)。

## 3. 渲染与加载策略
*   **流量驱动 (90/10 Rule)**: 构建时查询 Cloudflare Analytics，预渲染前 10% 页面（SSG），剩余 90% 走 Edge SSR 并入 KV 缓存。
*   **离线优先 (Offline-First)**: PWA/App 优先读取本地适配器数据，利用 `TanStack Query` 实现后台同步。

## 4. 零硬编码准则 (Zero Hardcoding)
*   **配置驱动**: API Endpoints、存储 Provider、功能开关全部由 `ConfigService` 统一分发。
*   **设计令牌**: 样式必须使用 Token（如 `theme.spacing.md`）。Web 端映射至 Tailwind，RN 映射至 StyleSheet。
*   **文案国际化**: 所有 UI 文本必须通过 i18n ID 引用。

## 5. 多端复用检查清单
- [ ] 核心 Action 是否能在无 DOM 环境（Node.js/RN）下运行？
- [ ] 存储操作是否完全被适配器接口屏蔽（无直接 SDK 导入）？
- [ ] 原生插件调用（如相机）是否具备 Web 环境的优雅降级逻辑？
```

---

### 文档 2：类型安全与跨端契约指南 (TYPE_SAFETY.md)

```markdown
# 类型安全与跨端契约指南 (Type Safety & Unified Contract)

## 1. 契约驱动开发 (Contract-Driven)
系统以 **Schema** 为核心契约，实现端到端的类型安全。
*   **单一真相源**: 在 `packages/shared` 定义业务实体、输入输出的 Zod Schema。
*   **端到端同步**: Web 端通过 tRPC 实现强类型同步；RN 端通过导入 Procedure 类型实现本地约束。

## 2. 统一结果对象模式 (Unified Result Pattern)
所有 API Action 和存储操作必须返回统一的 Result 包装器：
```typescript
type Result<T, E = AppError> = 
  | { 
      ok: true; 
      data: T; 
      source: 'server' | 'cache' | 'local' | 'bridge'; 
      synced: boolean; 
    } 
  | { ok: false; error: E };
```
*   **source**: 标识数据来源（服务器、缓存、本地数据库或原生桥接）。
*   **synced**: 标识本地变更是否已同步至云端。

## 3. 运行时边界校验
*   **零信任入口**: 所有的外部输入（URL 参数、存储读取、插件回调）必须经过 Zod Schema 校验。
*   **禁止 any**: 无法确定类型的数据必须声明为 `unknown`，并通过类型谓词 (Type Predicates) 转换。

## 4. 存储层类型契约
```typescript
interface IStorageAdapter {
  /** 泛型 T 确保获取的对象符合领域实体定义 */
  query<T>(collection: string, params: QueryParams): Promise<Result<T[]>>;
  
  /** 认证模块抽象，屏蔽 Cookie (Web) 与 SecureStore (RN) 的差异 */
  auth: {
    getSession(): Promise<Result<Session | null>>;
  };
}
```

## 5. 跨平台样式与 Token 类型化
```typescript
/** 设计令牌类型化，杜绝硬编码颜色/间距 */
type ColorToken = keyof typeof DesignSystem.colors;
type SpacingToken = keyof typeof DesignSystem.spacing;

// UI 组件的样式 Props 仅接受这些字面量类型
```

## 6. 类型安全检查清单
- [ ] 所有的 API 动作是否包含了输入/输出的 Zod 校验？
- [ ] 实体定义是否使用了平台无关的时间戳 (number) 和标识符 (string)？
- [ ] 是否在核心逻辑层外出现了厂商特定的类型定义？
- [ ] 虚拟列表 `Virtua` 的数据项是否具有严格的类型约束？

## 7. 质量红线
*   **平台污染**: `packages/core` 中禁止出现任何 Web API 或原生插件的直接类型引用。
*   **静默失败**: 所有的 Result 必须在 UI 层显式处理 `ok: false` 的情况。
```

---

### 💡 架构师总结

1.  **关于 PWA 增强**：通过整合 **Capacitor**，我们不仅拥有了 Web 的开发速度，还拿到了原生 App 的入场券（通知、离线存储）。
2.  **关于渲染极致**：**Virtua** 保证了无论是 Web、PWA 还是原生 RN，处理复杂健康数据列表时都能保持 60FPS。
3.  **关于未来迁移**：由于逻辑全部封装在 **Action 层**，存储全部封装在 **Adapter**，你从 PWA 彻底迁移到纯原生 RN 时，不需要重写任何一行业务代码。

**我们是否已经准备好，按照这个目录结构初始化 `packages/core` 和第一个存储适配器接口？**
